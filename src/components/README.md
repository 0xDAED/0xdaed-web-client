# 🧱 Component Architecture Guide

Компоненты организованы по уровням абстракции:

```
UI → Blocks → Features → Pages
```

(в рамках папки components используются первые три слоя)

---

# 📁 Структура папки components

```
components/
├── ui/
├── blocks/
├── features/
└── common/
```

---

# 🎨 UI — атомарные компоненты

Минимальные элементы интерфейса без бизнес-логики.

### Примеры:

- Button.vue
- Input.vue
- Modal.vue
- Card.vue
- Icon.vue

### Разрешено:

- props
- emit
- стили
- slot

### Запрещено:

- API вызовы
- store
- composables
- бизнес-логика

### Цель:

Создавать переиспользуемые визуальные примитивы.

---

# 🧱 Blocks — визуальные композиции

Компоненты, собранные из UI элементов.

### Примеры:

- UserCard.vue
- SidebarMenu.vue
- StatsGrid.vue
- FeatureList.vue

### Разрешено:

- использовать UI компоненты
- простые вычисления отображения

### Запрещено:

- API
- бизнес-логика
- прямые вызовы store

### Цель:

Формировать готовые куски интерфейса.

---

# 🚀 Features — функциональные компоненты

Компоненты, реализующие поведение и бизнес-логику.

```
features/
  auth/
  dashboard/
  projects/
```

### Примеры:

- LoginForm.vue
- UserStats.vue
- ProjectTable.vue

### Разрешено:

- composables
- API вызовы
- store
- сложная логика

### Цель:

Инкапсулировать функциональность фич.

---

# ♻ Common — универсальные элементы

Компоненты, используемые в разных частях проекта:

- Loader.vue
- ErrorView.vue
- EmptyState.vue
- Skeleton.vue

---

# 🔒 Правила зависимостей (строго)

```
UI ❌ ничего не импортирует
Blocks ➜ UI
Features ➜ Blocks + UI
Pages ➜ Features
```

🚫 Запрещены обратные зависимости
🚫 UI никогда не знает о логике

---

# 📦 Экспорт компонентов

В каждой папке используется `index.js`.

### Пример:

```
components/ui/index.js
```

```js
export { default as Button } from './Button.vue'
export { default as Input } from './Input.vue'
export { default as Modal } from './Modal.vue'
```

Использование:

```js
import { Button, Modal } from '@/components/ui'
```

---

# 🧭 Как определить уровень компонента?

Задай себе вопрос:

### ➤ Это просто элемент интерфейса?

→ `ui`

### ➤ Это визуальный блок без логики?

→ `blocks`

### ➤ Есть логика, данные, состояние?

→ `features`

### ➤ Это универсальный служебный элемент?

→ `common`
